# Basic Setting
set hidden true
set ignorecase true
set icons true


# Set lf to raanger like keybindings

#map S 


map gD cd ~/Downloads
map gm cd ~/quant_alpha_v5
map gf cd ~/quant_alpha_v5/notebook/factor/factors_tomato
map gp cd ~/play
map gc cd ~/.config
map gv cd ~/.config/nvim
map go cd ~/note/markdown
map gl cd ~/note/logseq


map S push :shell<enter>$SHELL<enter>  # 打开当前路径



# TODO : Add more keybindings
# TODO : add unzip command
# TODO : add unrar command
# TODO : add tar command
map gU unzip -o  # 解压文件


cmd mkdir ${{
    printf "Enter a directory name: "
    read ans
    mkdir $ans
}}



map a mkdir
map D delete  # 删除文件或目录
map x cut
